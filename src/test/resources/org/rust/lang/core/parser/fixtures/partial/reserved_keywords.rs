priv fn bar() {}

trait Trait {
    virtual fn foo();
}

impl Trait for Sstruct {
    override type Type = i32;
    final fn foo() {}
}

fn foo() {
    do {} while (true);

    let a: typeof(1) = 1;
}
